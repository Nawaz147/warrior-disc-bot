// const Discord = require("discord.js");
// const ms = require("parse-ms");
// const db = require("quick.db");

// module.exports = {
//   name: "xxx",
//   aliases: ["xxx"],
//   description: "To xxx",
//   usage: "xxx",
//   category: "Economy",
//   run: async (client, message, args) => {
//     const tokenDB = db.fetch(`${message.author.id}.oyOtoken`);

//     if (message.author.id == "768747976767832084") {
//       db.fetch(`key_${user.id}.${tokenDB}`);
//       db.add(`key_${user.id}.${tokenDB}`, 50);
//       message.channel.send("50 keys 🔑 added to your account");
//     } else {
//       message.channel.send("You are not allowed to use this command");
//     }
//   },
// };
